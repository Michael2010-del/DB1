
📝 **Programmer's Diary**
Flask • SQLAlchemy • SQLite • Веб-приложение • Блог-платформа

## 📖 О проекте
Персональный блог-платформа для программистов с возможностью создания, хранения и отображения записей. Приложение реализует полноценный CRUD (Create, Read) функционал с использованием базы данных SQLite. Интерфейс выполнен в современном темном стиле с адаптивным дизайном, идеально подходящем для технических записей и заметок.

## ✨ Основные возможности
- **📄 Создание записей** - форма с заголовком, подзаголовком и основным текстом
- **📁 Хранение в БД** - автоматическое сохранение записей в SQLite базе данных
- **📊 Отображение карточек** - сетка карточек для быстрого обзора всех записей
- **🔍 Детальный просмотр** - отдельная страница для полного чтения записи
- **🎨 Современный дизайн** - темная тема с акцентами синего цвета
- **📱 Адаптивная верстка** - корректное отображение на мобильных устройствах
- **🔗 Навигация** - удобные переходы между страницами
- **🔄 Динамическое обновление** - автоматическое обновление списка после создания

## 🏗️ Архитектура проекта
### Структура файлов:
```
programmers_diary/
├── main.py                      # Основное Flask-приложение
├── temp.py                      # Скрипт для создания БД
├── instance/
│   └── diary.db                 # База данных SQLite
├── static/
│   ├── css/
│   │   └── style.css            # Основные стили
│   └── img/                     # Изображения и иконки
│       ├── logo.svg             # Логотип приложения
│       ├── login.svg            # Иконка входа
│       └── post_icon.png        # Иконка поста (250x250)
├── templates/                   # HTML-шаблоны
│   ├── index.html              # Главная страница (все записи)
│   ├── card.html               # Страница одной записи
│   └── create_card.html        # Форма создания записи
└── requirements.txt            # Зависимости Python
```

### Модель базы данных:
```python
class Card(db.Model):
    id = db.Column(db.Integer, primary_key=True)          # Уникальный ID
    title = db.Column(db.String(100), nullable=False)     # Заголовок (до 100 символов)
    subtitle = db.Column(db.String(200), nullable=False)  # Подзаголовок (до 200 символов)
    text = db.Column(db.Text, nullable=False)             # Основной текст (неограниченно)
```

## 🚀 Установка и запуск
### Требования:
- Python 3.8+
- Flask
- Flask-SQLAlchemy
- SQLite3 (встроен в Python)

### Шаги установки:
1. **Клонируйте проект:**
```bash
git clone https://github.com/your-username/programmers-diary.git
cd programmers-diary
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
# Активация:
# Windows: venv\Scripts\activate
# Mac/Linux: source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install flask flask-sqlalchemy
```
*Или создайте requirements.txt:*
```txt
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
```

4. **Инициализируйте базу данных:**
```bash
python temp.py
# или в Python-консоли:
# from main import app, db
# app.app_context().push()
# db.create_all()
```

5. **Запустите приложение:**
```bash
python main.py
```

6. **Откройте в браузере:**
   - Перейдите по адресу: `http://localhost:5000`
   - Приложение готово к использованию!

## 📱 Использование приложения
### Главная страница (`/`):
```
Мои записи
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│ [Иконка]        │ │ [Иконка]        │ │   [+]           │ ← Создать новую
│ Заголовок 1     │ │ Заголовок 2     │ │                 │
│ Подзаголовок 1  │ │ Подзаголовок 2  │ │                 │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### Создание записи (`/create`):
```
[Форма создания записи]
┌─────────────────────────────────────┐
│ [Иконка поста]                      │
│ [Поле: Заголовок]                   │
│ [Поле: Подзаголовок]                │
│ [Текстовое поле: Основной текст]    │
│ [Кнопка: Создать]                   │
└─────────────────────────────────────┘
```

### Просмотр записи (`/card/<id>`):
```
Заголовок записи

┌─────────────────────────────────────┐
│ Подзаголовок                        │
│                                     │
│ Основной текст записи...            │
│ Может быть длинным и содержать      │
│ форматирование, код, заметки...     │
│                                     │
│ [Кнопка: Вернуться к записям]       │
└─────────────────────────────────────┘
```

## 🔧 Технические детали
### Маршруты Flask:
```python
@app.route('/')                          # Главная - все записи
def index():
    cards = Card.query.order_by(Card.id).all()
    return render_template('index.html', cards=cards)

@app.route('/card/<int:id>')             # Детальный просмотр записи
def card(id):
    card = Card.query.get(id)
    return render_template('card.html', card=card)

@app.route('/create')                    # Форма создания
def create():
    return render_template('create_card.html')

@app.route('/form_create', methods=['GET','POST'])  # Обработка формы
def form_create():
    if request.method == 'POST':
        # Создание новой записи
        card = Card(title=title, subtitle=subtitle, text=text)
        db.session.add(card)
        db.session.commit()
        return redirect('/')
```

### Настройка базы данных:
```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///diary.db'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False
db = SQLAlchemy(app)
```

### Инициализация БД:
```python
# temp.py
from main import app, db
app.app_context().push()
db.create_all()  # Создает таблицы в базе данных
```

## 🎨 Дизайн и интерфейс
### Цветовая схема (Dark Blue Theme):
```css
Основные цвета:
- #0F172A (темно-синий, фон)
- #F8FAFC (светло-серый, текст)
- #1E293B (сине-серый, карточки)
- #0EA5E9 (акцентный синий, рамки и кнопки)
```

### Кастомный скроллбар:
```css
body::-webkit-scrollbar {
    width: 12px;
    background: #0F172A;
}
body::-webkit-scrollbar-thumb {
    background-color: rgba(30, 41, 59, 0.4);
    border-radius: 20px;
}
```

### Структура карточки:
```html
<div class="card">
  <a href="/card/{{el.id}}">
    <img src="post_icon.png" alt="post_icon">
    <div class="card__text">
      <h2>{{el.title}}</h2>        <!-- Заголовок -->
      <p>{{el.subtitle}}</p>       <!-- Подзаголовок -->
    </div>
  </a>
</div>
```

### Адаптивный дизайн:
- **Сетка карточек**: flexbox с wrap для адаптации к разным размерам экрана
- **Отступы**: процентные значения для резиновой верстки
- **Медиа-запросы**: рекомендуется добавить для мобильной оптимизации

## 📊 Модель данных
### Таблица `Card`:
| Поле | Тип | Ограничения | Назначение |
|------|-----|-------------|------------|
| `id` | Integer | Primary Key, Auto-increment | Уникальный идентификатор |
| `title` | String(100) | Not Null | Заголовок записи (до 100 символов) |
| `subtitle` | String(200) | Not Null | Краткое описание (до 200 символов) |
| `text` | Text | Not Null | Основное содержимое записи |

### Пример записи:
```python
{
    "id": 1,
    "title": "Мой первый проект на Flask",
    "subtitle": "Создание веб-приложения с нуля",
    "text": "Сегодня начал работу над новым проектом...\nИспользовал Flask и SQLAlchemy..."
}
```

## 🚀 Возможности для расширения
### Функциональные улучшения:
1. **Полный CRUD**:
   - Редактирование существующих записей (Update)
   - Удаление записей (Delete)
   - Подтверждение удаления (модальное окно)
2. **Категории и теги**:
   - Система категорий для записей
   - Теги для организации контента
   - Фильтрация по категориям/тегам
3. **Поиск и фильтрация**:
   - Поиск по заголовку и содержимому
   - Сортировка по дате, популярности
   - Пагинация для большого количества записей

### Технические улучшения:
1. **Аутентификация**:
   - Регистрация и вход пользователей
   - Личные дневники разных пользователей
   - Система ролей (админ, пользователь)
2. **Расширенная БД**:
   - Добавление даты создания/изменения
   - Счетчик просмотров записей
   - Возможность комментирования
3. **Файловые вложения**:
   - Загрузка изображений к записям
   - Прикрепление файлов с кодом
   - Галерея изображений
4. **Экспорт/импорт**:
   - Экспорт записей в PDF/Markdown
   - Импорт из других форматов
   - Резервное копирование БД

### UX/UI улучшения:
1. **WYSIWYG редактор**:
   - Rich-text редактор для форматирования
   - Подсветка синтаксиса для кода
   - Предпросмотр Markdown
2. **Темы оформления**:
   - Переключение светлой/темной темы
   - Кастомизация цветовой схемы
   - Разные шаблоны отображения
3. **Мобильное приложение**:
   - PWA для оффлайн-доступа
   - Push-уведомления о новых записях
   - Синхронизация между устройствами

### Дополнительные функции:
1. **Статистика**:
   - Графики активности написания
   - Анализ наиболее используемых слов
   - Цели и отслеживание прогресса
2. **Социальные функции**:
   - Публикация выбранных записей
   - Подписка на других авторов
   - Лайки и закладки
3. **Интеграции**:
   - Экспорт в GitHub как блог
   - Синхронизация с Notion/Evernote
   - API для внешних приложений

## 🎯 Целевая аудитория
### Для программистов:
- **Ведение технического блога** - документация проектов
- **Заметки по обучению** - конспекты курсов и статей
- **Идеи и планы** - запись идей для будущих проектов

### Для студентов:
- **Конспекты лекций** - структурированное хранение материала
- **Подготовка к собеседованиям** - вопросы и ответы
- **Портфолио проектов** - демонстрация выполненных работ

### Для всех:
- **Личный дневник** - мысли и размышления
- **Планирование задач** - цели и прогресс
- **Творческое письмо** - рассказы и эссе

---

**Создано с 💻 для программистов, которые ценят свои мысли и идеи!**

---

*Совет для продакшена: Используйте PostgreSQL вместо SQLite для продакшена, добавьте миграции базы данных с помощью Flask-Migrate, настройте бэкапы базы данных и используйте WSGI-сервер для развертывания.*
